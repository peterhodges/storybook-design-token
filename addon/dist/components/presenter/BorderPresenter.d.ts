/// <reference types="react" />
import { Token } from '../../types/token.types';
interface BorderPresenterProps {
    token: Token;
}
export declare const BorderPresenter: ({ token }: BorderPresenterProps) => JSX.Element;
export {};
