/// <reference types="react" />
import { Token } from '../../types/token.types';
interface LetterSpacingPresenterProps {
    token: Token;
}
export declare const LetterSpacingPresenter: ({ token }: LetterSpacingPresenterProps) => JSX.Element;
export {};
