/// <reference types="react" />
import { Token } from '../../types/token.types';
interface LineHeightPresenterProps {
    token: Token;
}
export declare const LineHeightPresenter: ({ token }: LineHeightPresenterProps) => JSX.Element;
export {};
