/// <reference types="react" />
import { Token } from '../../types/token.types';
interface SpacingPresenterProps {
    token: Token;
}
export declare const SpacingPresenter: ({ token }: SpacingPresenterProps) => JSX.Element;
export {};
