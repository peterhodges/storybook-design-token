import { ReactNode } from 'react';
interface ClipboardButtonProps {
    button: ReactNode;
    value: string;
}
export declare const ClipboardButton: ({ button, value }: ClipboardButtonProps) => JSX.Element;
export {};
